# Сводка изменений в Code.gs

## Часть 1: Оптимизация кода (рефакторинг)

### ✅ Добавлены helper-функции:
1. `isUserCurrentlyRestricted(chatId, userId)` - проверяет, ограничен ли пользователь в данный момент
2. `buildSubscriptionKeyboard(config, userId)` - генерирует клавиатуру подписки с кнопками

### ✅ Заменён дублирующийся код:
- **handleMessage()** - проверка restrict-статуса → `isUserCurrentlyRestricted()`
- **handleMessage()** - генерация клавиатуры → `buildSubscriptionKeyboard()`
- **handleCallbackQuery()** - генерация клавиатуры → `buildSubscriptionKeyboard()`
- **applyProgressiveMute()** - генерация клавиатуры → `buildSubscriptionKeyboard()`

### ✅ Удален пункт меню:
- Удален `'🧪 Запустить тесты', 'runTestsFromMenu'`

## Часть 2: Функция удаления репостов

### ✅ Новая логика:
- Проверка репостов: `message.forward_from || message.forward_from_chat`
- Удаление репостов из неразрешённых источников
- Исключения: админы, whitelist, target_channel_id

### ✅ Система нарушений для репостов:
- **1-2 нарушение**: удаление + предупреждение
- **3+ нарушений**: удаление + прогрессивный мут
- Счётчик: `forward_violations_${userId}`

### ✅ Новые тексты:
- `forward_warning_text`: "в этом чате запрещены репосты..."
- `forward_mute_text`: "вы были ограничены на {duration} минут..."

### ✅ Новые конфигурационные параметры:
- `forward_check_enabled`: TRUE/FALSE
- `forward_violation_limit`: количество репостов перед мутом

### ✅ Новая функция:
- `applyProgressiveMuteForForwards()` - прогрессивный мут за репосты

## Сохранено без изменений:
- Константа `IGNORED_USER_IDS = ['136817688', '777000']`
- Монолитная структура файла
- Вся существующая функциональность